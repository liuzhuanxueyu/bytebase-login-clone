# Bytebase 登录演示

这是一个基于 React 和 Firebase 的现代化登录页面演示项目。

该项目复刻了 Bytebase 的登录页面 UI，并集成了 GitHub 第三方登录功能。

## ✨ 功能特性

*   **1:1 UI 复刻**:
    *   **桌面端**: 采用现代化的左右分屏设计，左侧为品牌展示，右侧为登录表单。
    *   **移动端**: 响应式设计，在手机上自动调整为单列布局，确保良好的用户体验。
*   **第三方登录**:
    *   集成 **Firebase Authentication**。
    *   支持 **GitHub** 账号一键登录。
*   **用户资料展示**:
    *   登录成功后展示用户头像、昵称、邮箱、UID 等详细信息。
*   **技术栈**:
    *   React 19
    *   TypeScript
    *   Vite
    *   Tailwind CSS 3
    *   Firebase SDK

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/liuzhuanxueyu/bytebase-login-clone.git
cd bytebase-login-clone
```

### 2. 安装依赖

```bash
npm install
```

### 3. 本地运行

```bash
npm run dev
```

打开浏览器访问控制台显示的地址 (通常是 `http://localhost:5173`)。

## 🛠️ 构建与部署

### 构建生产版本

```bash
npm run build
```

### 部署到 GitHub Pages

本项目已配置自动化部署脚本。

1.  确保 `package.json` 中的 `homepage` 字段已修改为你的 GitHub Pages 地址。
2.  运行部署命令：

```bash
npm run deploy
```

## 📝 项目亮点

- [x] 像素级还原登录页面 UI
- [x] 响应式设计 (适配 Mobile)
- [x] 文件结构清晰，代码规范
- [x] 使用 React + TypeScript
- [x] 集成 GitHub OAuth 登录
- [x] 支持 GitHub Pages 自动化部署

## 📂 目录结构

```
src/
├── components/
│   └── Login.tsx       # 登录页面组件 (UI 核心)
├── App.tsx             # 主应用组件 (处理路由和 Auth 状态)
├── firebase.ts         # Firebase 配置文件
├── index.css           # Tailwind CSS 入口
└── main.tsx            # React 入口
```

## 📄 许可证

MIT
